plugin: aws_ec2
regions: 
  - us-east-1
filters:
 tag:Name:
   - Node0
   - Node1
   - Node2
instance-state-name : running
compose:
  ansible_host: private_ip_address
  ansible_user: "{{ 'ubuntu' if (platform_details == 'Linux/UNIX') else ('ec2-user') }}"